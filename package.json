{
  "name": "layr-hp",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/clean-build.mjs && node scripts/build-seo-corporate-catalog.mjs",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "npm run build && bash scripts/gate-regression.sh",
    "check:cloudflare": "wrangler deploy --dry-run",
    "dev:cloudflare": "npm run build && wrangler dev",
    "preview:cloudflare": "wrangler versions upload",
    "deploy:cloudflare": "wrangler deploy"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.6",
    "jose": "6.2.12",
    "remark-directive": "^4.0.0",
    "unist-util-visit": "^5.1.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "miniflare": "5.20260903.0-alpha",
    "wrangler": "4.129.0"
  }
}
