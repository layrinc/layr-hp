# エルトリ 全国地域LP — テンプレート修正版の検証

2026-09-17。元のサービスLPを共通テンプレートとした1,965地域LPの検証記録。以前の8ページの地域・業種ガイド案を置き換える。

## 結果

| 確認 | 結果 |
| --- | --- |
| 静的ビルド | 2,016ページ生成。既存50＋全国一覧1＋都道府県47＋市区町村・行政区1,918 |
| 全件テスト | 4テストPASS。全1,965地域の固有title・description・H1・canonical・Service/FAQ/パンくず・サイトマップ・内部リンク・CTA引継ぎを検査 |
| 全地域の導線 | 全国一覧は全地域、都道府県LPは県内全地域へリンク。少数地域だけを選ぶ生成制限なし |
| 元LPとの共通化 | 全地域に元LPのセクションを保持。料金・画像・プラン・資料リンクは同じ正本から取得 |
| 既存デザインゲート | 全項目で基準値から悪化なし。サービス専用 `ltori.css` に変更なし |
| Cloudflare dry-run | 成功。静的アセット4,216ファイルを読み込み。アップロードはしない検査 |
| 画面 | 名張市1280 / 768 / 390px、橋本市、三重県、全国一覧、元LP、長い地域名を確認。横はみ出しなし、最小文字12px |
| インタラクション | 4つの活用シーンのキーボード切替、モバイルメニューとEscapeを確認 |
| 計測・フォーム | CTAは地域付きselect_content。成功時のみgenerate_lead。通知/メール下書きに地域名。未知sourceを除外 |
| JSなし | 本文・見出し・相談リンクを静的HTMLから表示 |
| 通信 | 実問い合わせ・実GA送信なし。フォームは成功/失敗をモックし、日本語フォントをローカル配信して検証 |

[ブラウザ検証結果](browser-results.json)。画面資料は依頼者との会話で共有。画像を公開リポジトリへ添付する処理は自動承認審査で拒否されたため、ソースのPRには追加していない。

## 配信確認

CloudflareプレビューとGitHub CIの結果・確認用URLはPR #31に記録する。本番公開には `AGENTS.md` の既存規則に基づく河出さんの承認・マージが必要。ローカルWorkersは環境の `uv_interface_addresses` エラーで起動できないため、この画面検証はAstroが生成した静的HTMLをローカルHTTPサーバーで配信して実施した。

Search Consoleの実際のインデックス、検索順位、GA4の管理画面設定、問い合わせ・受注の実績は今回の技術検証の対象外。ページの生成成功をそれらの成果として扱わない。
