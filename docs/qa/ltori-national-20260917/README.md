# エルトリ 全国集客基盤 — 初回レビュー

2026-09-17 / モードA / 比較元 main `6df6d85`。

## 結果

静的ビルド・ブラウザでの新規ページ検証はPASS。本番未公開。Workers実行環境の確認は保留。Cloudflareプレビュー、原稿・サービス条件の承認、GA4/Search Console管理側の設定確認を公開前に行う。

| 確認 | 結果 |
| --- | --- |
| `npm run check` | 58ページ生成。既存デザインゲートの悪化なし |
| `node --test tests/ltori-seo.test.mjs` | 4件PASS。全国データ整合、下書き除外、不完全原稿の拒否、8ページのcanonical・サイトマップ・内部リンク、相談元パラメータ |
| `npm run check:cloudflare` | dry-run成功。アップロードなし |
| Workersローカル配信 | 環境上の`uv_interface_addresses`エラーで起動不可。Cloudflare上で末尾スラッシュ・既存転送・404を確認する必要あり |
| 新規CSSのdesign-gate | G1〜G10 / G16〜G17の12項目すべてPASS |
| 見出し・構造 | H1とmain各1、パンくず、Service構造化データ。架空のLocalBusinessを生成しない |
| 表示 | 1440 / 1280 / 768 / 390 / 375pxで横はみ出しなし。日本語フォントで確認 |
| 新規領域の文字 | 最小14px、計測した通常状態の最低コントラスト4.54:1 |
| モーション | hoverの移動なし。reduced-motion対応。スクロール演出の追加なし |
| フォーム | 成功/失敗をモック。成功時のみgenerate_lead、表示名を通知/メール下書きへ引継ぎ。未知のsourceを除外 |
| 通信 | 実問い合わせ・実GA送信なし。外部通信を遮断して検証 |
| JavaScriptなし | 本文と相談リンクを表示できる |

新規CSS単独では、Baseから読み込まれる共有トークンをゲートが解決できないため、実際の`style.css`の`:root`定義と新規CSSを併せて監査した。定義を検査用に捏造・追加していない。ブラウザでも色・文字サイズが解決されることを確認した。

上下の余白は非対称。フォームの送信成功は営業上の有効相談・受注を意味しない。LINEへ移動後の相談成立は今回のコードだけでは追跡できない。

## 画面

全国ハブ1280 / 768 / 390px、名張市1440 / 375px、製造業1280pxの画面を確認済み。画像の公開添付は保留し、レビュー用の画面資料を依頼者との会話で別途共有する。

- [ブラウザ検証結果](browser-results.json)

## 既存資産

元のサービスLPは最終CTA付近の案内リンク追加、共通フッターはリンク1件追加。既存のフォームUI、LPのFV、サービス価格、共通トークンを変更していない。従来の全体ゲートには許容済みの基準超過があるため、既存全体を新規基準で合格としたものではない。

## 公開前の確認

初期原稿は公的な地域情報とLAYRの活用提案。地域の支援実績や数値成果としては掲載していない。原稿の最終確認後にPRを承認し、既存Cloudflareでプレビュー確認と本番反映の確認を行う。公開直後はGA4 DebugViewとSearch ConsoleのURL検査を少数ページで確認する。
