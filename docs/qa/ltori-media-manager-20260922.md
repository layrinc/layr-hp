# メディア管理ツール検証 — 2026-09-22

## 対象

`https://seo.layr.co.jp/media/`。既存Cloudflare Accessの署名検証後に、保護下の `/tools/ltori-seo/media/` HTMLを提供する。公開メディアの変更は別PR #38で反映済み。

## 実装した範囲

記事企画・検索需要CSV・アクセス実績・接続/入出力の4画面。元120企画IDを維持し、潜在層候補を追加。検索ボリュームはレポート単位の条件を保存して降順表示。0/10/空欄/範囲値を区別する。Google連携はGA4/GSCの読み取りのみ、シート読み取りは別の任意権限。

このブラウザへの保存とJSONバックアップに対応する。編集しても公開記事は変更されない。端末間自動同期・定期無人取得はない。

## 認証・データ

- 認証なしのroot/media/HTML alias/encoded aliasを拒否。preview/別hostでの生成元HTMLは403。publicの一般メディアは従来どおり公開。
- 認証後のmedia alias正規化、HEAD、POST拒否、no-store/noindex、cache validator除去を検証。
- 初期企画は保護HTMLにのみinline。公開 `_astro` client bundleへの企画タイトル混入とsitemapへの管理経路混入を自動検査。
- Google tokenをbackup/DBへ保存しない。アクセス実績と市場検索数を区別。GA4不明値は0補完しない。ユーザー数を記事横断で合計しない。
- 実APIの部分失敗、タイムアウト、401/403/429、ページング、URL/sourceの正確な紐づけをmockで検証。
- 公開終了・slug変更後も過去編集/実績を復元できる。現在の公開記事への新規紐づけは厳密に確認する。
- 選択ファイル/調査条件変更後の古い非同期結果が、復元・取込対象にならないことを検証。

## ブラウザ確認

ローカル検証serverは `/tmp/ltori-manager-preview.py`、loopbackにのみbind。session.jsonは `local-qa@example.test` の検証fixture。これはrepo/本番には含まない。実Cloudflare認証を突破してUI検証していない。

- 制作状況・メモの編集→保存→reloadで保持を確認。検証後に元の制作状況へ戻した。
- ローカル用4行CSVで、0のみ黒地/白字、10の保持、空欄未取得、10〜100範囲表示を確認。降順と0絞り込みも確認。検証用データは本番seedには含めない。
- 未接続のGA4/GSCは未取得表示で、取得ボタン無効。実績列に架空データを置かない。
- 375pxで接続/入出力フォーム横はみ出しなし、主要ボタン高さ44px以上。

## 実データ接続の状態

ラッコキーワードの契約内一括調査を既存ログインで実施。日本/日本語、2025-09〜2026-08、GoogleLive、過去データ補完OFF。取得元の0表示は無需要の断定ではない。

GA4/GSCの実取得は未確認。既存管理サイトはIAB/ChromeともCloudflare Accessのメールログイン画面。本人へログインを依頼済み。OAuthクライアントIDと数字のGA4プロパティIDは認証後に既存設定を確認する。API mock成功と実アカウントの連携完了を混同しない。

## 最終結果

- 68ページのAstroビルド、全115テスト、デザイン回帰ゲートPASS。
- 追加UIのデザイン機械ゲート12項目PASS（新CSS＋利用中トークン）。
- Cloudflare dry-run成功。新規bindingや秘密情報の追加なし。
- 200語の取得値：59語が10以上、141語が表示0、未知/範囲0件。元120IDは維持。追加80は調査候補で公開月未定。
- 採用LINE/採用 LINE/LINE採用の各480は合算せず統合検討。内定辞退/面接辞退には企業向け意図確認を条件付け。
- 新規ブラウザoriginから初期企画200/取得済み200、検索数降順を確認。0絞り込み141件、1以上59件を確認。
- 1280/768/390/375pxすべてbody横はみ出しなし、h1は1。表の横スクロールは表内に限定。
- ブラウザconsole error/warnなし。
- 独立レビューMedium2件（過去記事の復元、非同期ファイル取込競合）を修正し、回帰テスト成功。

画面: `ltori-media-manager-20260922/workspace-{1280,768,390,375,zero-1280}.png`。画像内メールはローカル検証用fixtureです。
