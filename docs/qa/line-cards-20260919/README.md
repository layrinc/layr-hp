# LINE支援カード・FAQ / 2026-09-19 / モードA

> この記録は初回のHTML作例版。ユーザーの追加指示によるCanva画像版の最新確認は [Canva画像差し替えレビュー](../line-cards-canva-20260919/README.md) を参照。

## 変更内容

- トップの「LAYRができること」とLINEサービスLPの支援内容を6枚カードへ変更。
- 参考画像の端末画面→支援内容→短い便益→詳細開閉の構成を、LAYRの緑白に適用。
- 画面はHTMLの作例。各端末と一覧下に作例と明記。実在顧客や架空成果数値は未使用。
- FAQを「料金・契約」「相談・構築」「配信・運用」の3カテゴリへ再編（2 / 4 / 2問）。既存のFAQを統合し、表示とJSON-LDを同じデータから生成。
- CSSは専用ファイルに集約。共通テーマ・ヒーロー・料金表・CTAの変更なし。
- 先行する資料請求ページの更新はそのまま保持。

## レビューで効いている点

- カードの表示順と高さを揃え、各支援内容を見比べられる。
- 長い説明は左揃えの開閉領域に分離。最初の一覧を短く保つ。
- FAQはnative details。カテゴリボタンに矢印・Home・End対応を追加し、JSなしでは3カテゴリすべてを表示する。
- 実際のサービス説明を図示し、根拠のない数値・他社素材を追加しない。

## 検証

| 対象 | 結果 |
|---|---|
| npm run build | PASS、57ページ |
| node --test tests/line-support.test.mjs | PASS、7テスト |
| scripts/gate-regression.sh | PASS、既存共通CSSの悪化なし |
| 新規CSS＋依存する既存:rootのdesign-gate | PASS、12項目 |
| 2ページ×1440 / 1280 / 768 / 390 / 375px | 横はみ出し0、3 / 3 / 2 / 1 / 1列 |
| 端末作例の収まり | 全6点が指定の240px高さ以内 |
| カード開閉 | クリックで開く、Enterで閉じる |
| FAQ | 全カテゴリ切替、回答開閉、矢印とHomeのキー操作を確認 |
| FAQ構造化データ | 両ページとも表示と8問すべて一致 |
| JSなし | ビルドHTMLで全パネルがhiddenなし、回答はnative details（静的検証） |
| h1 | 各ページ1つ |
| タブのタップ領域 | PC56px、スマホ約50pxの高さ |
| 文字コントラスト | 新規部分の主要ペア最小4.87:1（濃緑文字／淡緑背景） |
| ブラウザのJSエラー | 0 |
| git diff --check | PASS |

### 定量ゲート

影0、中央揃え3、font-size値9種、800/900ウエイト0、直値hex0、2px以上の単色border0、未指定easing0、focus-visible2、変数循環0・未定義0。

単体CSSだけのゲートでは継承トークンが未定義扱いになるため、既存style.cssの:root宣言を抽出して結合した検証用CSSに対し再実行。追加の独自色や仮トークンで回避していない。CIの7テストにもトークン参照チェックを含める。

## 既存部分と未実施

- 既存のグローバルCSS負債、旧LPのスタイル・数値・料金は今回の是正対象外。
- CMSの編集UIは変更しない。既存title/desc・q/aを使用し、表示補助フィールドを追加。カスタマイザーの簡易プレビューは今回の新レイアウトに未対応。
- JSを無効化した実ブラウザでの操作は未実施。SSRマークアップを自動テストで検証。
- 1440/1280pxのDOM実測は完了。ブラウザパネルのキャプチャ上限により、これらのPNGは右側が切れる場合がある。全幅の目視用は1100px、タブレット768px、スマホ375pxを参照。
- 本番公開・push・PR作成・外部フォーム送信は未実施。CTAは既存のまま、新しいCV導線を追加していない。

Blocker / High: 今回の差分に該当なし。

PASS
