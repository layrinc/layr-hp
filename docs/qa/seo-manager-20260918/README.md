# エルトリ 全国SEOキーワード管理の検証

2026-09-18。比較元 `565df617`。ユーザー指定の末尾地域リンクを削除し、`/tools/ltori-seo/` に運用ツールを追加。

## 実装・確認結果

| 対象 | 結果 |
| --- | --- |
| ビルド | 2,017ページ。地域LP1,965、全国一覧1、既存50、管理ツール1 |
| 地域LP | 全地域で「ほかの対応地域」と直下の戻るリンクを削除。追加指示により冒頭のパンくず行・地域LPのBreadcrumbList・相談欄とフッターの全国一覧リンクも削除。町域例・都道府県の子地域一覧・全国索引・サイトマップは維持 |
| 台帳 | 同じ地域正本から1,965地域、標準9,825KW候補を生成。検索需要・順位は未測定として扱う |
| 対応管理 | 優先度、状況、手動インデックス状態、担当者、期限、次の作業、メモ。地域の一括更新。KW追加・保留 |
| CSV | 日本語/英語のSearch Consoleページ別・クエリ別CSV、BOM、改行、引用符、0と未取得の区別。期間・条件別保存と同一条件の置換。対象外行を提示 |
| 実績の範囲 | サイト全体のクエリ実績と対象LPの実績を区別。異なる期間やページ/クエリの数値を合算しない |
| 保存 | IndexedDB、再読み込み後の保持、JSON復元、CSV出力、古いタブからの上書き防止。保存不能時は閲覧のみとして通知 |
| 保存上限 | 復元可能な20MB以内に制限。最大60実績・100,000実績行。数値や文字列形式を検証 |
| 自動テスト | 14件PASS。`node --test tests/ltori-seo.test.mjs tests/seo-manager.test.mjs` |
| ブラウザ | 管理一覧1280/768/390/1440/375、実績画面1280/768/390、KW一覧1280、詳細1280/390の11画面。横はみ出しなし、文字12px以上、JS例外なし |
| LP画面 | 名張市の更新後対応エリア欄を確認。削除対象2種のリンク群なし |
| デザイン | 既存トークンと組み合わせた新規CSSの12ゲートPASS。全srcの既存違反に悪化なし、フォーカス定義は29→31。共通ゲート合格 |
| 配色 | 新規使用5組、最低4.81:1。元のサービスLPの色・画像・価格を維持 |
| Cloudflare | dry-run成功。4,224アセット。Worker・DNS・認証・本番配信設定は変更しない |
| クローラー | noindex/nofollow/noarchive、サイトマップ除外、Workers AssetsのX-Robots-Tagを設定。公開LPは検索対象を維持 |

ブラウザの確認はPlaywrightで同じビルド成果物をローカル配信して行った。CSVの数値・作業メモは動作検証用であり、実績を示さない。共有画像は未編集・未取り込み状態から取得した。実送信、Googleへの自動クエリ、順位の自動取得は行っていない。

[画面と動作](browser-results.json) / [デザインゲート差分](gate-comparison.json) / [コントラスト](contrast.json)。画像は1280/768/390を含めて依頼者との会話で共有する。以前の公開画像アップロードの自動承認拒否を踏まえ、公開リポジトリにスクリーンショットを追加していない。

## 制約と公開

管理ツールの編集はブラウザ内保存。クラウド同期、Google認証、Search Console API、自動順位取得は未接続。編集内容をLPやGitHubへ送る機能ではない。移行・端末変更・URL変更時はJSONバックアップを復元する。管理画面のnoindexはアクセス制限ではないが、配信データに業務メモ・取り込み済み実績は含めない。

プレビューとCIの最新URL・コミットはPR #31を参照。閲覧ツールでCloudflareプレビューが拒否されるため、実配信側の画面・ヘッダー・リダイレクトは未確認。本番はAGENTS.mdで定められた承認・マージ後。

## 2026-09-18：地域LPのパンくず削除

比較元 `6736ef24512cc01a5bac64eb3e0a8088897713bd`。依頼画像の鳥羽市LPに合わせ、全地域で冒頭のパンくず行を削除した。地域LPでは相談欄・共通フッターの全国一覧リンクも表示しない。共通フッターの指定は地域LPからだけ渡し、元のサービスLPや他ページのリンクは維持する。管理ツールの保存・操作ロジックは変更していない。

- クリーンビルド2,017ページ、既存の自動テスト14件PASS。全地域で対象の表示とBreadcrumbListがなく、固有title・H1・canonical・Service/FAQ・サイトマップ・相談導線が維持されることを確認。
- 共通デザインゲートの悪化なし。追加スタイルはなく、パンくず専用の未使用CSSを削除。
- 鳥羽市の1280 / 768 / 390pxを実画面で確認。ヒーローが最初の本文要素となり、全国・三重県への上位リンクなし。横はみ出しなし、最小文字12px、JS例外なし。三重県LPの市区町村一覧は維持。
- Cloudflare dry-runは4,224アセットを読み込み成功。画像は依頼者との会話で共有し、公開リポジトリには追加しない。

[今回の画面確認結果](breadcrumb-browser-results.json)。初回のローカルビルドには以前の地域HTMLが残ったため、出力フォルダーを退避してクリーンビルドした成果物で上記を検証した。Cloudflareの実配信表示は引き続き未確認。
