# 市単位公開・アクセス実績の検収（2026-09-18）

対象は既存エルトリLPとMode D管理画面。既存の色・フォント・角丸を再利用し、表示している指標をデータの取得状況と区別した。市ごとの設計例を同じLP構成へ追加し、公開対象と未公開候補の違いを台帳へ明示した。

## 検証結果

- ビルド：55ページ、地域LPは名張市・鳥羽市・橋本市のみ。全1,965地域レコードの出力有無・サイトマップ・索引を公開台帳と照合。下書き・対象外のHTMLは残らない。
- `node --test tests/ltori-seo.test.mjs tests/seo-manager.test.mjs tests/ltori-analytics.test.mjs`：21件PASS。
- 共通デザイン回帰ゲート：基準からの悪化なし。
- 全srcの差分比較：G1 200 / G2 165 / G3 72 / G4 0 / G5 39 / G6 41 / G7 151 / G8 47 / G9 24 / G10 31 / G16 2 / G17 0。変更前と同値。既存の閾値超過は今回変更しない。
- Cloudflare dry-run：静的アセット242ファイル、アップロードなしの構成検証。
- 画面：1280 / 768 / 390、追加1440 / 375。h1は1、文字12px以上、ページ全体の横はみ出しなし。新規表は表の内側だけスクロール。使用する配色は既存 `contrast.json` と同じで最小4.81:1。
- ブラウザ操作：対象839件／公開3件／下書き836件／旧対象外1,126件、CSVの確認・保存・リロード・出力・バックアップ、同期間のGSCとの結合、Google認証/APIのローカル模擬応答、再読込時の認証解除を確認。
- 問い合わせ：外部通信を遮断して模擬応答で確認。HTTP 200でもJSONが失敗なら完了イベント0回。成功なら専用イベント1回、名張市sourceを保持。本番ホスト以外ではGAタグを起動しない。
- 地域LP：冒頭パンくずと全国一覧への誘導は表示しない。参考文献は市公式情報、設計と配信文面はLAYRの作例。

詳細は `release-browser-results.json`。画像は会話に添付して保存し、公開GitHubへの画像アップロードは行わない。

## 未実施・接続条件

Googleの実アカウントは未認証。実GA4プロパティID・OAuthクライアント初期設定が必要。実データ取得、Search Consoleへのサイトマップ送信、GA4本番DebugViewによるイベント到達確認は未実施。機能はAPIの正常・空・部分失敗・権限エラーの模擬応答で検証した。フォームの実メール送信は行っていない。Cloudflareプレビューへの外部閲覧はツールの接続制約があるため、同じビルドを手元で検収する。

Blocker / High: 0（上記のGoogle実接続は未完了として画面・報告に明示）。

PASS
