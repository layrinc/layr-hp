# スマホ横スワイプ・カード簡素化 検収

2026-09-19 / モードA（既存の緑白） / layr-design-review

## 対象と変更範囲

- トップ `/` と LINEサービス `/service/line/` の共通カード。
- 実装: `src/components/LineSupportCards.astro`、`src/styles/line-support.css`。
- 検証: `tests/line-support.test.mjs`。
- 600px以下は横スクロール＋スナップ。カード幅90%で次のカードを少し見せる。
- 項目名の下の大きな便益コピーと画像拡大リンクを削除。画像・項目名・「詳しく見る」で開く説明を維持。
- 共通テーマ、FAQ、カードデータ、画像ファイル、他セクションは変更しない。

## 結果

- 判定: PASS（今回の変更範囲）。2026-09-19、河出さんの「反映して」により本番公開を承認。公開前にCloudflareプレビューとCIを確認する。
- `npm run build`: 58ページ成功。既存のmarkdown-remark設定の非推奨警告のみ。
- `node --test tests/line-support.test.mjs`: 8/8 PASS。
- `bash scripts/gate-regression.sh`: 既存基準からの悪化なし。
- 差分デザインゲート: 12項目 PASS。実際のstyle.cssの`:root`定義と変更CSSを結合して変数継承も検証。影0、中央揃え2、フォントサイズ8種類、直値hex0、未定義変数0。
- `git diff --check`: PASS。

## ブラウザ検証

Chromeの画面幅変更で確認。実機Safariでの検証ではない。

| 対象 | 画面幅 | カード配置 | ページ全体の横はみ出し |
| --- | --- | --- | --- |
| トップ | 320 / 375 / 390 / 600px | 1行・カード内だけ横スクロール | 0px |
| トップ | 601 / 768px | 2列 | 0px |
| トップ | 1280 / 1440px | 3列 | 0px |
| LINEサービス | 375px | 1行・カード内だけ横スクロール | 0px |
| LINEサービス | 768px | 2列 | 0px |
| LINEサービス | 1440px | 3列 | 0px |

- 390pxで右矢印キーによる横移動・カード位置へのスナップを確認（scrollLeft 0 → 320.5px）。
- 6枚目まで到達可能。scrollLeft 1569px = 最大スクロール量。末尾カード右端と表示枠右端はサブピクセルの丸め差0.18px以内。
- 6枚の画像の読み込み成功を確認。
- 「数値分析・レポート」の詳細を開閉し、説明が表示されることを確認。画像拡大リンク0件。
- キーボード操作のフォーカス表示あり。見出しは項目名のh3を6件維持。
- モバイル案内は600px以下のみ表示。ページの縦スクロールを制限するtouch-action指定なし。
- 横移動・詳細開閉はブラウザ標準機能。新規JS・自動再生・別スクロール制御なし。JSなしの静的HTMLで全カード・詳細・FAQ回答が残ることをテスト。
- h1は各ページ1件、既存の上下余白非対称・reduced-motionを維持。CV導線・計測の変更なし。

## 目視

- モバイルでは次のカードが少し見える。項目名と「詳しく見る」の間に不要な説明文はない。
- PC・タブレットは既存の列構成を維持。カード高だけ余白に合わせてコンパクトに調整。
- 既存の色・書体・角丸を使用。スマホのキーボードフォーカス枠は検証操作による表示。
- スクリーンショット: `home-375.png`、`home-768.png`、`home-1280.png`、`home-1440.png`、`home-390-last.png`、`line-375.png`、`line-768.png`、`line-1440.png`。
