# 最終公開前レビュー / モードA / 2026-09-19

## 変更と承認

ユーザー指定により、6枚の画像直下にあった「資料掲載例｜〜」をすべて削除。画像・サービス見出し・便益コピー・詳細開閉・拡大リンクは維持する。共通テーマと本文は変更しない。

この修正後の本番公開は2026-09-19の会話で明示承認済み。先行する資料請求の差し替え、LINE支援カード、カテゴリFAQを含む本タスクの改修を公開する。

## 効いている点

- 画像と支援内容の見出しが直接つながり、補助キャプションの重複が解消。
- Canvaの実図版6点、画像の縦横比と拡大リンクを維持。
- モードAの緑白・既存トークンを維持し、色・書体の追加なし。

## 検証

- 最新main `27ba848` を取り込み、管理画面のCloudflare Access保護を保持。
- `npm run build`: 58ページ生成。
- LINEカード・FAQ、地域SEO、管理画面、認証保護の全38テストPASS。
- 共通CSS回帰ゲートPASS。
- 新規CSS＋既存:rootのdesign-gate: 全12項目PASS。影0、中央揃え2、font-size9種、直値hex0、未定義変数0。
- 1440 / 1280 / 768 / 390 / 375px: 横はみ出し0、キャプション0、画像6点を保持。
- 1280 / 768 / 390pxの最終スクリーンショットを同フォルダに保存。
- カードのクリック／Enter開閉を確認。両ページの静的HTMLでキャプション不在・画像6点・拡大リンク6点を自動検証。
- `npm run check:cloudflare`: dry-run PASS。既存WorkerとASSETS bindingを使用。
- 既存の本番コミットに対するWorkers Builds成功を確認。DNS・認証ポリシー・Worker構成の変更なし。

## Blocker / High

今回の差分に該当なし。

## 既存資産・未実施

サイト全体の既存CSS・料金・法務・統計表現は対象外。JS無効時はSSRの静的検証で代替。外部フォーム送信は未実施。

PASS
