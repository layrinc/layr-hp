# エルトリメディア計測の確認 / 2026-09-21

比較元: e7b1818（PR #35）。モードA。CSS、料金、記事本文、管理画面の変更なし。

## 今回の修正

- 記事と一覧のCTAにsourceを付与。本文の対象リンクにも同じsourceを付与。
- 公開記事から許可リストを生成し、相談・資料請求のメールに記事名を含める。
- GA4の相談・資料請求を区別し、資料請求は受付応答のsuccessを確認してから計上。
- サンクスページの直接表示・再読込によるリード計上を廃止。
- 送信中・受付済みの二重送信を防止。送信失敗時は入力内容を保ち、再試行とメールへの引き継ぎが可能。
- 計測スクリプトの遮断・例外があっても、受付成功表示と資料ダウンロードへ進む。

## 検証

- `npm run build`: 62ページ。
- `node --test tests/*.test.mjs`: 58件成功（既存48件＋今回10件）。
- フォームのテストは生成済みHTMLの実インラインJSをVMで実行。送信とGA4通信は模擬。
- 最終レビューで応答本文の停止中にタイムアウトが解除される問題を再現。JSON読取・成功判定までタイムアウトを維持し、8秒後の入力復帰とメールへの引き継ぎを追加テストで確認。
- `bash scripts/gate-regression.sh`: 全項目で基準悪化なし。
- Company-Brainのdesign-gate.sh: メディアCSS＋実際の共通CSSから抽出したトークン定義の依存範囲で12項目PASS。CSS単独では共通変数10件を未定義と判定するため、依存元を含めて検査した。共通CSS全体の既存逸脱はサイト側の回帰ゲートで確認。
- `npm run check:cloudflare`: Wrangler 4.129.0のdry-run成功。アップロードなし。
- Chromeで記事・相談・資料請求を1280/768/390pxで確認。横スクロールなし、h1各1つ。記事は追加で1440/375pxも確認。
- 本文リンクから相談フォームへ進み、source付きURLと採用LINEの選択状態を確認。資料CTAでもsource付きURLと流入元フィールドの存在を確認。フィールド値と個人情報を含まないGA4ペイロードはVMテストで検証。
- スクリーンショット9枚とbrowser-results.jsonを本フォルダに保存。
- ブラウザログに拡張機能由来のSentryエラーが1件。対象サイトのコードの例外ではない。

## 本番の読み取り確認

- メディア一覧と3記事は200、canonicalは本番URL、noindex指定なし、GA測定IDあり。
- sitemap-index.xmlとsitemap-0.xmlは200。メディア4URLを収録。robots.txtからサイトマップへ案内あり。
- Googleログイン可能。既存LAYRプロパティのリアルタイムに記事名と `ltori_media_cta_click` が表示されることを確認。確認操作はテストトラフィックとして扱う。
- Search Consoleのドメインプロパティと既存サイトマップ登録を確認。メディア一覧は「Googleに認識されていません」。ライブテストは「URLはGoogleに登録できます」「ページをインデックスに登録可能です」。インデックス登録完了とは区別する。

## 実施していないこと

- 実際の問い合わせメール送信・受信確認。
- Googleのカスタムディメンションやキーイベント設定の変更。
- 記事のAI生成API、自動投稿、自動集計・定期実行。

## デザインレビュー

既存の読み物構成、CTA、フォームを維持し、表示と計測を切り分けた。新規の色・書体・CSS値なし。今回の差分にデザインBlocker/Highなし。

PASS
