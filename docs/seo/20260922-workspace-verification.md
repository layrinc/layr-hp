# SEOワークスペース統合の検証記録

2026-09-22。ローカル実装の検証記録。本番への反映・本番データ変更は未実施。

## 機能・データ

- ビルド成功：74ページ。
- 自動テスト：292件成功、失敗0。認証・同origin移行ページ・URL分類・既存地域保存・新しい公式メディア保存・競合・取得範囲・欠損と0・取得失敗時の保持を含む。
- Cloudflareの公開前コンパイル確認成功。`--dry-run`のみで、アップロードなし。既存D1・既存取得先を継続。
- 独立レビューで、旧公開ドメインの移行ページ保持、公開待ちが承認済み／予約済みだけになることを再確認。残るBlocker・Highなし。
- `git diff --check`成功。

## ブラウザ

本番・Googleへ接続しないローカル検証サーバーを使用。架空のアクセス実績である旨を全画面に表示。以下の数値確認は本番の成果を示さない。

- 概要・公式メディア・戦略ページを1440／1280／768／390／375pxで確認。画面全体の横はみ出しなし。表は領域内で横スクロール。
- 共通メニューから全国SEO／採用ノート／公式メディア／共通の公開・運用／戦略へ移動でき、選択表示が一致。
- 旧rootの `#analytics` は `/regional/#analytics` へ移動し、アクセス・問い合わせの画面を選択。
- 公式メディア23記事から検索・改善状況で絞り込み。管理用の優先度・改善状況・根拠・メモを保存し、再読込後に保持。
- 競合応答では保存せず、入力と競合メッセージを保持。ブラウザによる未保存破棄の確認は自動操作で応答を確認できなかったため、手動確認項目として残す。
- 実測0と未取得「—」を別表示。未接続・API失敗時に架空の0を表示しない。
- Google取得失敗では保存済み成功実績と失敗表示を併記。
- 公式メディアの保存データ取得失敗時は編集ボタンを無効化。
- 旧URLからの計測画面移動でブラウザコンソールエラーなし。
- 全国SEOの地域一覧は自動GSCを既定表示。アクセス画面で自動GA4／GSCを切り替え、架空の12PV／2クリック・40表示・5位が取得元別に表示されることを確認。自動値の削除は無効、手動履歴と共有stateへの追加・上書きなし。
- 独立レビューで、CTRのクリック欠損が0％になる既存計算を修正。欠損「—」と実測0％を回帰テストで確認。

## デザイン検収：モードD

効いている点：共通ナビで現在の施策がわかる。公開URLの範囲と指標の意味を併記し、3施策の混同を防ぐ。未取得を明示して運用判断を誤らせない。

- 新規CSS・ページ＋依存トークン定義の機械ゲート：12項目PASS。
- 既存共通スタイルの回帰ゲート：悪化なし。既存資産の一斉是正は行わない。
- 新規ページのh1は各1。ナビのタップ高さ44px。フォーカス枠と動きを抑える設定あり。
- 実表示の文字／補助文字／リンク色を取得し、背景とのコントラストは順に13.90／8.62／5.04。新規の色・影・書体は追加していない。
- 認証された管理ツールのため、公開LPのCVボタン検査は対象外。

## 本番反映後に確認すること

統合PRは https://github.com/layrinc/layr-hp/pull/47 。初回のCIとCloudflareプレビュービルドは成功。保護されたプレビューURLは認証前403であり、保護設定は変更していない。最終修正のチェック後にマージし、公開結果はPRへ追記する。

- 本番Access認証下で3施策を切り替え、既存地域データを読み込めること。
- 次のGA4・Search Console取得後に、公式メディア `/media/` が取得範囲へ追加されたこと。
- 採用ノートの企画は既存どおり端末保存。公式メディアの改善メモはサーバー共有。企画すべての端末間同期は今回の実装に含まない。
- 市場検索ボリュームの自動調査は取得元が未接続。AIOの引用状況も自動計測として表示しない。

PASS
