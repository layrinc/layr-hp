# レビュー: 全国市LPの公開予定 / モードD / 2026-09-23

対象: `src/pages/tools/ltori-seo/growth.astro`、`src/lib/seo-manager/growth-app.mjs`。

## 効いている点

- 今日の都道府県と47日分の予定表を分け、最初から47行を展開しない構成。
- カレンダー終了と全市公開完了を区別し、未公開・準備未完了を表示。
- 自動調査・制作の状態、準備済み数、要確認の市を、公開実績とは別に表示。
- 既存のパネル、表、summary、フォーカスを利用し、CSS・色・書体の追加は0件。

## 指摘

コード上のBlocker / Highなし。実画面の1280 / 768 / 390px検証も完了。ドキュメント幅は1265 / 753 / 375pxで横はみ出しなし。

## 機械・動作確認

| 確認 | 結果 |
|---|---|
| UIのVMテスト | 6件PASS（47都道府県792市、日程終了、未取得と0、停止、承認、制作状況） |
| 既存ナビゲーションテスト | 3件PASS |
| JS構文、diff空白検査 | PASS |
| 共通デザイン回帰ゲート | 全12項目悪化なし |
| UIで使用するCSSと正本変数のデザインゲート | 全12項目PASS |

ゲート対象は `style.css` の既存`:root`宣言、`seo-manager.css`、`seo-growth.css`、今回のAstroを連結した `/tmp/ltori-prefecture-ui-gate.css`。G1/G2/G3/G4/G5/G6/G7/G8/G9/G16/G17は0、G10のfocus-visibleは2。Astro単体ではimportされたCSSをゲートが追跡しないためG10が0になるが、実際のスタイルでは既存フォーカス定義が適用される。共通の全サイトCSSは回帰ゲートで確認し、既存負債を変更していない。

VMテストは実際のクライアントモジュールを読み、AstroのIDから構成したDOMへ描画。HTML文字列を挿入しないこと、全47行を描画すること、再描画時に古い行や要確認リストを残さないことも確認。

## 未実施

- 本番の実データ画面はAccess認証が必要。ローカル検証用データで3幅・47行の先頭北海道と末尾沖縄を確認。画像はscreenshots/prefecture-rollout-{1280,768,390}.jpg。
- Astro build 74ページとWrangler梱包検査はPASS。
- 全サイトのコントラスト再測定: 既存トークン・CSSを無変更で利用。新規配色なし。

コードと機械検証: PASS
