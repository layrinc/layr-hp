# tokens.md — 4モードのトークン全表

## 0. 読み方

- **モードAの正本はサービス概要資料（PDF・全24ページ）**。`/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/06_デザイン_CDO/成果物/20260816_LINE構築運用サービス資料_トンマナ正本.pdf`
- **この表にない値を新規に足さない。** 派生が要るときは変数を増やさず `color-mix(in srgb, …)` / `oklch()` をその場で書く。
- 優先順位は **河出さんの明示指示 > 資料PDFの実測値 > 対象プロジェクトの既存トークン > このファイルの基準値**。
- 各表は2列。**「新規の基準」＝これから作るものに適用する値**／**「既存資産の現状」＝実測値**。ズレている箇所は一斉是正が未承認という意味で、既存側を勝手に直さない（ズレ一覧は §7）。
- モードは1プロジェクト1つ。ページ内で切り替えない。B/C/Dは対象が決まっているときだけ発動する。迷ったらA。B/Cのトークンは**ページscoped変数**（`--edit-*` / `--wa-*`）で持ち、グローバル `:root` を上書きしない。
- 資料内の**例示画像**（LINEトーク画面・スプレッドシート・フォーム見本・分析レポート）に写る色はトンマナではない。トークン化しない。

## 1. プロジェクト別トークン正本の位置

| 順位 | 対象 | モード | 正本ファイル（絶対パス） |
|---|---|---|---|
| **0（最上位）** | **LAYRブランド全体** | **A** | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/06_デザイン_CDO/成果物/20260816_LINE構築運用サービス資料_トンマナ正本.pdf` |
| 1 | layr.co.jp 本体 | A | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/layr-hp/src/styles/style.css` の `:root`（17〜93行）。**「現状値」であって正本ではない。資料とズレている**（§7）。**実効値は末尾の再定義が勝つ** |
| 1 | 同・管理画面テーマ | A | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/layr-hp/src/data/theme.json` |
| 2 | AI導入LP | B | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/layr-hp/src/pages/service/ai.astro` の `<style>`（416行〜） |
| 2 | よろず屋マーケくんLP | C | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/layr-hp/public/yorozuya/index.html` の `:root`（18〜31行） |
| 3 | Growthダッシュボード | D | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/業務効率化AIツール/Growthダッシュボード/public/index.html` の `:root`（12行）。**116行で再宣言あり**（§6-6） |
| 3 | LAYR HUB | D | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/業務効率化AIツール/B2Bハブ/public/index.html` の `:root`（2026-09-03サイドバー型へ全面リニューアル・D新基準準拠・ゲートG1-G17全PASS。ログイン画面のみ `src/worker.js` 内に旧Apple調 `:root` が残存） |
| 3 | バナラボ | D | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/04_広告運用事業_CAO/バナー生成ツール/banner-creator/app/globals.css`（Next.js。ツール固有の青は既存維持・新規では作らない） |
| 3 | 記事ふやす君 | D | `/Users/kawadeikkan/Desktop/Company-Brain/株式会社LAYR/業務効率化AIツール/SEO記事ツール/kiji/public/index.html` の `:root`（アクセントはブランドグリーン `#00BF63`・新基準準拠。ゲートG1-G17全PASS 2026-08-22） |

`theme.json` は管理画面から `style.css` の6変数（`--green` `--green-dark` `--black` `--bg` `--radius` `--font-en`）を上書きする。**この6本を経由しない直値をLP側に書かない。**

## 2. モードA — 資料トンマナ（標準・LAYRの顔）

**2026-08-18に全面差し替え。** 旧A（ティール `#0F9E96` 単色・グラデーション禁止・影なし）は破棄。以下が正。

### 2-1 色（役割名つき・すべて資料からの実測値）

| 役割 | 変数 | 実測hex | 使いどころ |
|---|---|---|---|
| ブランドグリーン（主役） | `--brand` | `#00BF63` | セクション連番・見出し左の縦罫・FAQ質問帯・CTA塗り・アイコン円の塗り・キーワード1語の色替え |
| グラデ開始（ティール） | `--grad-a` | `#0499AF` | グラデの左端／始点。単色では使わない |
| グラデ終了（ライトグリーン） | `--grad-b` | `#77D55C` | グラデの右端／終点。単色では使わない |
| 波形 淡緑 | `--wave-a` / `--wave-b` | `#C1E392` → `#B6DD75` | 表紙・裏表紙の角の有機曲線 |
| 黄（下線帯） | `--underline` | `#F2C650` | 表紙見出し直下のアンダーバーのみ |
| オレンジ（推奨） | `--recommend` | `#E9A32F` | 料金表「おすすめ」リボン。比較表の最上位列ヘッダー |
| 強調バッジ黄 | `--badge-y` | `#F6CC01` | 「約20倍」「約6倍」の円バッジ |
| 強調赤 | `--badge-r` | `#FF5757` | 「9,600万人」の円・ステップ番号の丸。**1画面に1〜3箇所まで** |
| 淡水色（面） | `--tint-cyan` | `#D6EEF1` | 料金カードの地・タグチップの地 |
| 淡緑（面） | `--tint-green` / `--tint-green2` | `#DFF6EB` / `#E7F8F0` | 薄い緑の面（濃／淡の2段） |
| 濃緑（文字） | `--green-ink` | `#008516` | 緑の強調文字（白地で `--brand` のコントラストが足りない時） |
| 本文 | `--text` | `#333333` | 本文全般 |
| 見出し | `--head` | `#000000` | 大見出し・カード見出し・数字 |
| 補助・非強調 | `--muted` / `--muted2` | `#A6A6A6` / `#9D9D9D` | 補助文字・比較表の下位列ヘッダー・グラフの比較対象 |
| 地・紙 | `--paper` | `#FFFFFF` | 全ページの地 |
| ダークアイコン円 | `--icon-dark` | `#4A4A4A` | 課題・ネガ文脈のアイコン円の塗り |

**未実測（資料内に存在するが hex 未計測）**: 比較表の見出し列の淡黄緑、比較表 中位列ヘッダーの青。**この2つは新規で使わない。** 必要になったら資料から実測してこの表に追加する。勝手に近似色を発明しない。
**役割の配分**: 主役は `--brand` の1色。黄・赤・オレンジ・淡水色は**役割が固定されたサブ**であり、その役割以外に転用しない。1ビューポート内で `--brand` の塗りは最大3箇所（帯・CTA・アイコン円）、サブ色のバッジは合計3箇所まで。

### 2-2 グラデーション（LAYRの正規意匠）

| 用途 | 定義 | 適用対象 |
|---|---|---|
| 標準（横） | `linear-gradient(90deg, #0499AF 0%, #77D55C 100%)` | ラベル帯（ピル）・番号バッジ（円）・カードの枠線・三角矢印 |
| 円周（斜め代替） | `linear-gradient(135deg, #0499AF 0%, #77D55C 100%)` | 円形フローの正円の枠。`border` にはグラデを直接置けないので、外側divに `background` でグラデを敷き `padding` 分だけ内側の白円を重ねるか、`mask` で抜く |
| 波形 | `linear-gradient(135deg, #C1E392 0%, #B6DD75 100%)` | 表紙・裏表紙の角の有機曲線（SVGパスの `fill`） |

- **禁止**: AI由来の紫→青（`#7C3AED`→`#2563EB` 系）、3色以上の多段グラデ、`radial-gradient`、見出し文字へのグラデ（`background-clip:text`）。グラデは**面と線にだけ**使い、文字は必ず単色。グラデ面の上の文字は白 `#FFFFFF` / 700 固定。

### 2-3 型スケール（7段。ここにないサイズを使わない）

| 段 | 新規の基準 | 資料での現れ方 | 既存資産の現状（style.css） |
|---|---|---|---|
| display | `clamp(40px, 5.6vw, 72px)` / 700 / lh 1.15 | セクション連番「01」（`--brand`）・表紙見出し | `.hero2-title` `clamp(32px,5.3vw,62px)`（2535行で4回目の再定義） |
| h2 | `clamp(26px, 3.2vw, 38px)` / 700 / lh 1.4 | セクション見出し（黒）・締め行「>> ○○」 | `.sec-jp` `clamp(24px,3.2vw,36px)` |
| h3 | `20px` / 700 / lh 1.5 | カード見出し・プラン和名 | `.blk-h2` `clamp(21px,2.8vw,28px)` / weight 800 |
| lead | `19px` / 400 / lh 1.85 | ページ冒頭のリード文 | `clamp(15px,1.6vw,20px)` 等が混在 |
| body | `17px` / 400 / lh **1.9** | カード本文・回答文 | `16px` / `--lh-body:1.85` |
| sm | `14.5px` / 400 / lh 1.8 | 円形フローの説明文・注記「※」 | 14 / 14.5 / 13 / 13.5 が併存 |
| label | `12px` / 700 / tracking `.08em` | ラベル帯・タグチップ・英字プラン名 | 10.5〜12.5px が併存 |

- **書体は和文極太ゴシック1系統**。見出し・本文・英数字すべて `Noto Sans JP`（Poppins は廃止済み・復活させない）。ウエイトは **400 / 500 / 700 の3値のみ**。資料の見た目は900相当だが、Webでは実グリフのある700までとし、**サイズと余白で強さを出す**（800/900指定は合成太字＝にじむ。現状89箇所）。
- `letter-spacing` は **`0` / `.01em` / `.08em`（ラベル）/ `.2em`（ラベル帯・締め行の字間広め）/ `-.01em`（display）の5値まで**（現状20種）。
- 数字の単位は本体より2段小さくする（例: 「25**万円**」＝ display + sm）。`font-size` は既存の `calc(◯px * var(--fs,1))` の書き方を踏襲する（外すとCMSの文字サイズ拡縮が効かない）。

### 2-4 余白スケール（4層。**中間値を作らない**）

| tight（ラベル⇔見出し） | group（段落間・カード内） | block（カード間・ブロック間） | section（セクション上下） |
|---|---|---|---|
| `6px` | `22px` | `56px`（広い場合 `96px`） | 上 `clamp(88px, 9.5vw, 168px)` / 下 `clamp(56px, 6vw, 104px)` |

- セクションの上下paddingは**非対称（≒1.6:1）**。`padding-block` で上下を別に書く。28 / 32 / 38 / 44 / 46px のような**中間値を新規に足さない**。資料の型では見出し直下と締め行「>> ○○」の上を `block` 以上空ける。**見出しと本文をくっつけない。**
- 既存の現状: `section{padding: var(--sec-pad) 0}` ＝ `clamp(56px,6vw,92px)` で**上下同値**。ほかに `--sec-pad-s: clamp(32px,4vw,52px)` / `--gutter: clamp(16px,4vw,28px)` / `--maxw: 1120px`。zigzag系だけ `--zz-maxw:1180px` / `--zz-pad-t: clamp(66px,8vw,116px)` / `--zz-pad-b: clamp(100px,12vw,176px)` を別に持つ。

### 2-5 角丸

| | 小（タグ・入力） | 標準（ボタン） | カード・パネル | 円（バッジ・アイコン） | pill |
|---|---|---|---|---|---|
| **新規の基準** | `8px` | `12px` | `16px` | `50%` | `999px` |
| **既存の現状** | `--r-s` ＝12.6px | `--r` ＝23.8px | `--r-lg` ＝35px | — | `--r-pill` 一致 |

角丸の集合は **8 / 12 / 16 / 999px の4値＋円 `50%`**（`guidelines/design-system.md` §3-1 と同値。ゲートG6は ≤5種）。

`--radius` の現状は **28px**（`theme.json.radius:28` と同値）。**LP4本はいずれもこの28pxを使っていない**（ai 3px / yorozuya 8〜12px / _partner 14〜20px / line 12〜18px）。28px→16pxの引き下げは §7 で承認待ちなので**既存の `--radius` を勝手に書き換えない**。新規制作では上の基準値を直接使う。

### 2-6 影・easing・hover・強調

| 項目 | 新規の基準 | 既存資産の現状 |
|---|---|---|
| 影 | **`0 2px 12px color-mix(in srgb, #000 6%, transparent)` の1本のみ。** 資料のカードは極薄・遠い影。これ以外を足さない | `--sd-s` / `--sd` / `--sd-lg` / `--sd-green` の4本、`box-shadow` 宣言は実測88（`box-shadow` を含む行は98行） |
| 影の例外 | モーダル／固定ヘッダーのみ `0 8px 40px color-mix(in srgb, #000 10%, transparent)` | `--sd: 0 4px 20px 8%`。**新規で参照しない** |
| border-width | **単色は 1px（罫線）と 3px（見出し左の縦罫・Qバッジの白リング）の2値。** 円形フローとカードのグラデ枠は `border:4〜6px solid transparent` ＋ `padding-box`/`border-box` の二重背景で持ち、単色を太らせない（ゲートG8の除外対象） | `.btn{border:2px}` `.reason-card` アイコン `{border:4px}` `.sec-jp::after{height:4px}` |
| easing: UI即応 / 移動 | `180ms` / `240ms cubic-bezier(.22,.61,.36,1)` ／ `cubic-bezier(.65,0,.35,1)` | easing未指定（＝`ease`）が28件 |
| easing: reveal | `600ms cubic-bezier(.16,1,.3,1)` ＋ **60msスタッガー** | `.reveal{transition:opacity .6s ease, transform .6s ease}` |
| **hover** | **浮かない。** `transform` を使わず `border-color` を1段濃く／グラデ枠を不透明度100%に。CTAは `--brand` を1段暗く | `transform:translateY(-…)` ＋ 影の付け替えが多数 |
| **強調** | 下の5手段のみ。マーカー禁止 | `mark.mk-y`（`--mark-y`）が併存 |

**強調の5手段（資料由来。これ以外を作らない）**
1. キーワード**1語だけ**を `--brand` に色替え（文全体を色にしない）
2. 円バッジ `--badge-y`（倍率・比率）／`--badge-r`（最重要の1数値）に白の極太数字
3. 淡水色チップ `--tint-cyan`（成果タグ・対象者）
4. グラデのラベル帯（章の小見出し）／`--brand` のピル帯（FAQ質問・箇条見出し）
5. `--recommend` のリボン（料金表のおすすめプラン。1画面1個）

`transition` に **easingを必ず書く**（省略＝`ease` は禁止）。`prefers-reduced-motion: reduce` で `transition:none` に落とす分岐を必ず持つ（`style.css` 219行に実装あり）。スクロール表示は**既存の1機構に乗る**（現状3流派: 共通IO `.reveal→is-visible` / ai.astroも `is-visible` / yorozuyaは自前IOで `on` 付与）。`text-align:center` は **H1と最終CTAと締め行の3箇所まで**。`:focus-visible` を必ず定義する（現状 style.css 実測0）。

### 2-7 構造パターン11型（資料から言語化。新規レイアウトはこの中から選ぶ）

| # | 型 | 構成 |
|---|---|---|
| 1 | セクション見出し | 特大の連番（display / `--brand` / 700）＋ `--brand` の縦罫（幅3px・見出しより上下に伸ばす）＋ 日本語見出し（h2 / `--head` / 700）。連番と縦罫と見出しは横一列 |
| 2 | 締め行 | セクション末尾に「`>>` ○○○」を中央または左に置く。h2 / `--head` / 700、キーワード1語だけ `--brand` |
| 3 | ラベル帯 | 角丸ピル（`999px`）、標準グラデ（横）、白文字700、tracking `.2em`、左上。章の小見出し |
| 4 | 円形フロー | 大きな正円（枠4px・135度グラデ・塗りは白）を三角矢印（グラデ塗り）でつなぐ。円の中に太字の工程名、円の下に sm の説明文。3〜4個まで |
| 5 | 番号付きカード | カード左上に円バッジ（グラデ塗り・白数字）を**カードの角に重ねて**配置。カードは白地＋グラデ枠線・角丸16px |
| 6 | アイコン円 | 正円の塗り（`--brand` ＝ポジ文脈 / `--icon-dark` ＝課題文脈）＋白の線画アイコン。**カード上端に半分重ねる** |
| 7 | 料金カード | 地 `--tint-cyan`、英字プラン名（label / `--brand`）＋和名（h3を1段上げた特大 / `--head`）＋対象者ピル（白地・角丸999px）＋価格（display・数字特大／単位は sm）＋箇条書き。おすすめ列だけ左上に `--recommend` のリボン |
| 8 | FAQ | `--brand` の角丸ピル帯に白文字700の質問、帯の左端に円バッジ「Q1」。直下に `--head` の回答（body） |
| 9 | 比較表 | 見出し列は淡黄緑（**hex未実測・新規で使わない**）、列ヘッダーで序列を示す（下位＝`--muted` / 上位＝`--recommend`）。可否は `◯` / `−` の2記号だけ |
| 10 | 事例ページ | 左に正方形に近い写真、右に h2 見出し＋ body 本文＋ `--tint-cyan` のタグチップ群（2段まで） |
| 11 | 表紙／裏表紙 | 白地＋内側に細い枠罫（1px）、左上と右下に波形グラデの有機曲線、大見出し（display）＋`--underline` の下線帯＋右にアイソメトリックイラスト。社名は下部に小さく |

## 3. モードB — エディトリアル（明示指示制）

Aとの関係: **Aの代替であって上位ではない。** AI導入LPという単一ページの専用意匠。他ページへ持ち出さない。実装例: `src/pages/service/ai.astro`。以下の「現状」列はすべてこのファイルの実測値。

### 3-1 色

| 役割 | 新規の基準（変数名 `--edit-*`） | ai.astro の現状（直値） |
|---|---|---|
| 地・紙 | `#FCFAF5` | `.hv{background:#FCFAF5}` |
| 地・淡面 | `#F8F5EE` | `.sec.is-paper{background:#F8F5EE}` |
| カード | `#FFFFFF` | `#fff` |
| 本文 | `#23272E` | `#23272E`（20箇所） |
| 補助文字 | `#5D6672` | `#5D6672`（13箇所） |
| 罫線 | `#E2DCCE` | `#E2DCCE`（15箇所） |
| アクセント（主） | インクブルー `#24466E` | `#24466E`（16箇所） |
| アクセント（従・訂正） | 朱 `#C94F35`。**訂正・取り消し・「おすすめ」バッジ限定。本文色に使わない** | `#C94F35`（21箇所） |
| ダーク面の地 | `#20344E` | `.fin{background:#20344E}` |
| ハード影の色 | `#1B3355` | `3px 3px 0 #1B3355` |
| マーカー | `#F5E3A8` | `linear-gradient(transparent 62%, #F5E3A8 62%)` |

### 3-2 型スケール（7段）

| 段 | 新規の基準 | ai.astro の現状 |
|---|---|---|
| display | 明朝 `clamp(34px, 5vw, 52px)` / lh 1.5 | `.hv-h1` `clamp(30px,5vw,52px)` |
| h2 | 明朝 `clamp(32px, 3.6vw, 34px)` / lh 1.65 | `.h2` `clamp(23px,3.6vw,34px)` ← **下限が32px未満**。明朝は32px以上でのみ使う規約に反するので新規では踏襲しない |
| h3 | ゴシック `18px` / 700 | `.sv-body h3` 明朝 18px |
| lead | `17px` / lh 2.2 | `.hv-lead` `14.5px` / lh 2.2 |
| body | `17px` / lh **2.0** | `13〜14px` / lh 2.0〜2.15 |
| sm | `14px` / lh 2.0 | `12.5px` |
| label | `12px` / 700 / tracking `.06em` | `.hv-tag` `12px` / `.fl-osusume` `10.5px` |

**見出し明朝は32px以上でのみ使用**（`"Hiragino Mincho ProN","Yu Mincho","Noto Serif JP",serif`。Webフォント追加なし）。それ未満の見出し・ラベル・数字はゴシック（`Noto Sans JP`）。本文サイズはA同様17pxが基準で、既存LPの13〜14.5pxは**既存の現状**として新規で真似しない。

### 3-3 余白スケール（4層）

| | tight | group | block | section |
|---|---|---|---|---|
| **新規の基準** | `6px` | `20px` | `34px`（広い場合 `64px`） | 上 `clamp(96px,9vw,148px)` / 下 `clamp(60px,7.5vw,104px)` |
| **ai.astro の現状** | 6〜10px | 14 / 18 / 20 / 26px | 34px | `.sec{padding: clamp(60px,7.5vw,104px) 0}` ＝**上下同値** |

### 3-4 角丸・影・easing・hover・強調

| 項目 | 新規の基準 | ai.astro の現状 |
|---|---|---|
| 角丸 | **`3px` の1値**（pillは `999px` のヘッダCTAのみ） | 999 / 16-16-16-4（吹き出し）/ 10 / 8 / 6 / 3 / 2px が混在 |
| 影 | **ハード影 `3px 3px 0` の1値**（色は濃色 `#1B3355` 系）。ぼかし影を使わない | `3px` / `5px` / `6px` / `7px` / `8px` の5段 ＋ ダーク面のみ `0 18px 44px rgba(0,0,0,.35)` |
| easing（UI） | `150ms cubic-bezier(.22,.61,.36,1)` | `.15s`（easing未指定＝`ease`） |
| easing（描画演出） | `500ms cubic-bezier(.6,0,.3,1)` | `.td-strike` が同値で実装済み |
| reveal | `700ms cubic-bezier(.16,1,.3,1)` | `.reveal{transition:opacity .7s ease, transform .7s ease}` |
| **hover** | **沈む** `transform: translate(2px,2px)` ＋ 影 `3px 3px 0` → `1px 1px 0` | `.btn-ink:hover` が同値で実装済み |
| **強調** | 淡黄マーカー `linear-gradient(transparent 62%, #F5E3A8 62%)`。**開始位置62%固定** | 62%で統一済み。ダーク面のみ `rgba(201,79,53,.55) 62%` |

マーカーの開始位置は**62%以外を作らない**（社内資産全体では58/62/66/68/70%の5種が混在している）。

## 4. モードC — 和風（明示指示制）

Aとの関係: **よろず屋マーケくんの専用意匠。** 資料トンマナのグリーン・グラデを持ち込まない。実装例: `public/yorozuya/index.html`。「現状」列はこのファイルの実測値。

### 4-1 色

| 役割 | 新規の基準（変数名 `--wa-*`） | yorozuya の現状 |
|---|---|---|
| 地 | 和紙 `#F5EEDA` | `--washi:#f5eeda`（ただし `body{background:#fff}`） |
| 地・カード | `#FBF6E7` | `--washi-card:#fbf6e7` |
| 本文（墨） | `#332F26` | `--ink:#332f26` |
| 補助文字 | `#6D6555` | `--muted:#6d6555` |
| 罫線 | `#D9CBA4` | `--line:#d9cba4` ／ `--line-strong:#c4b184` |
| アクセント（主） | 藍 `#1D3A4D` | `--ai:#1d3a4d` ／ `--ai-deep:#152936` |
| アクセント（従） | 朱 `#B23C22` | `--shu:#b23c22` ／ `--shu-dark:#93301a` |
| 金差し | `#B78A2F`。**線と記号だけ。面で塗らない** | `--kin:#b78a2f` |

### 4-2 型スケール（7段）

| 段 | 新規の基準 | yorozuya の現状 |
|---|---|---|
| display | Zen Maru Gothic **700** / `clamp(30px, 4.4vw, 46px)`（Google Fontsで500;700;900を読込済だが**新規は700だけ**） | 見出し全般 `font-weight:900` ← **合成太字** |
| h2 | Zen Maru Gothic 700 / `clamp(24px, 3.2vw, 34px)` | `.section-title` 系 |
| h3 | Zen Maru Gothic 700 / `20px` | — |
| lead | Noto Sans JP 400 / `17px` / lh 1.95 | `1.1rem` 前後 |
| body | Noto Sans JP 400 / **16px / lh 1.95** | `16px` / lh **1.95** 一致 |
| sm | `14px` | `.9rem` |
| label | Zen Maru Gothic 700 / `12px` / tracking `.2em` | `.section-label{font-size:.9rem; letter-spacing:.2em}` |

### 4-3 余白スケール（4層）

| | tight | group | block | section |
|---|---|---|---|---|
| **新規の基準** | `6px` | `20px` | `52px` | 上 `clamp(88px,9vw,136px)` / 下 `clamp(56px,6vw,88px)` |
| **yorozuya の現状** | 5〜9px | 20 / 28px | — | `section{padding:88px 0}` ＝**上下同値・clamp なし** |

### 4-4 角丸・影・easing・hover・強調

| 項目 | 新規の基準 | yorozuya の現状 |
|---|---|---|
| 角丸 | カード `12px` / ボタン `8px` / 小 `4px` / 丸 `50%`（判子・番号のみ） | `--radius:12px` とボタン `8px` は一致 |
| 影 | **ハード影 `3px 3px 0` の1値**（色は `rgba(29,58,77,.15)` ＝藍の薄め） | `3px` / `4px` / `5px` / `6px` の4段 ＋ `inset 0 0 0 2px` の二重枠 ＋ `inset 0 -6px 0` の暖簾影 |
| easing | `150ms cubic-bezier(.22,.61,.36,1)` | `.15s ease` |
| **hover** | **浮く** `transform: translate(-1px,-1px)` ＋ 影 `3px 3px 0` → `5px 5px 0` | `.btn:hover{transform:translate(-1px,-1px)}` ＋ 影 `4px`→`6px` |
| **強調** | 朱の**文字色替え**のみ | 同左 |

C の不可侵（差し戻し実例あり）: **書体・文言・数字表記・セクション名・Q&Aの表記を「和風にする」目的で変えない。** 変えてよいのは色・装飾・紋様・カード造形・影・枠・余白・モチーフ・レイアウトだけ。読み手は地方の社長で、可読性が最優先。

## 5. モードD — 社内ツール

Aとの関係: **資料トンマナのグラデ・波形・バッジは持ち込まない。** ツールは道具であって営業資料ではない。営業相手に見せる画面だけ `--brand` を名乗りに使う。実装例: `業務効率化AIツール/Growthダッシュボード/public/index.html`。「現状」列はこのファイルの実測値。

### 5-1 色

| 役割 | 新規の基準 | Growth の現状 |
|---|---|---|
| 地 | `#FAFAF9` | `--bg:#FAFAF9` |
| パネル・カード | `#FFFFFF` | `--panel:#FFFFFF` ／ `--nav:#FFFFFF` |
| 本文 | `#1A1A18` | `--ink:#1A1A18` |
| 補助文字 | `#6E6E73` | `--mut:#8C8C88` ／ `--navmut:#6E6E69` ／ `--faint:#B4B4B0` の3段 |
| 罫線 | `#EDEDEA` | `--line:#EDEDEA` ／ `--line2:#F4F4F1` |
| hover面 | `#F2F2EF` | `--navhov:#F2F2EF` |
| アクセント | **無彩色。LAYRとして名乗るものだけ `#00BF63`（資料のブランドグリーン）** | `--teal:#0F9E96` ／ `--tealbg:#EAF6F4` ← **資料とズレ**（§7） |
| 状態: 良 | `#22C55E` | `--green:#22C55E` |
| 状態: 注意 | `#D97706` | `--amber:#D97706` → **116行目で `#B45309` に再宣言。初期値がデッドトークン** |
| 状態: 危険 | `#DC2626` | `--red:#DC2626` |
| 状態: 情報 | `#7C6FF0` | `--violet:#7C6FF0` |

判定: **LAYRとして名乗る（営業相手に画面を見せうる）なら `#00BF63`、自分の道具だけなら無彩色。** ツール固有の業務色（バナラボの青、経理まとめ君の紺）は**既存は維持、新規では作らない**。警告色は**1系統だけ**（現状Growth内だけで notice `#FFFBEB` と alert `#FFF7E8` の2系統。新規はどちらかに寄せる）。

### 5-2 型スケール（7段）

| 段 | 新規の基準 | Growth の現状 |
|---|---|---|
| display | `26px` / 700 | `26px` 一致 |
| h2 | `17px` / 700 | `17px` 一致 |
| h3 | `15px` / 700 | 14〜14.5px |
| lead | `14px` / 400 | 14px |
| body | `13.5px` / 400 / lh **1.65** | `13.5px` / lh **1.7** |
| sm | `12px` | 12 / 12.5px |
| label | `10.5px` / 700 | 10 / 10.5 / 11 / 11.5px の4種 |

現状の `font-size` 実測は12種（10 / 10.5 / 11 / 11.5 / 12 / 12.5 / 13 / 13.5 / 14 / 14.5 / 17 / 26px）。**新規は上の7段に収める。** 書体は system font（`-apple-system` 系）または `Noto Sans JP`。**Dモードのsystem fontは意図的な選択なのでOK**（他モードで `Inter` `Roboto` `system-ui` を「選択」として使うのは禁止）。

### 5-3 余白スケール（4層。画面密度が高いので値を圧縮する）

| | tight | group | block | section（パネル群の切れ目） |
|---|---|---|---|---|
| **新規の基準** | `6px` | `16px` | `24px` | `40px` |
| **Growth の現状** | 6 / 8px | 14 / 16 / 18px | 16 / 22 / 24px | `margin-bottom:16px` |

Dはページではなく画面。section層は「パネル群の切れ目」に使い、上下非対称の規約はDには適用しない。

### 5-4 角丸・影・easing・hover・強調

| 項目 | 新規の基準 | Growth の現状 |
|---|---|---|
| 角丸 | **小 `4px` / 標準 `8px` / カード `12px` / pill `999px`** の4値 | 2 / 4 / 6 / 7 / 8 / 9 / 10 / 12 / 14 / 16 / 50% / 999px が混在（kpi 14px・panel 16px・modal 16px） |
| 影 | **`0 1px 2px rgba(20,20,18,.04), 0 1px 3px rgba(20,20,18,.03)` の1本のみ**。他に足さない | `--shadow` と同値。薄く近い＝UIとして正しい |
| 影の例外 | モーダルのみ `0 12px 40px rgba(0,0,0,.18)` | 同値 |
| easing（UI） | `120ms cubic-bezier(.22,.61,.36,1)` | `.12s`（easing未指定） |
| easing（バー・数値の伸長） | `600ms cubic-bezier(.16,1,.3,1)` | `.bar i{transition:width .6s}`（easing未指定） |
| **hover** | **背景色の微変化のみ**（`background: #F2F2EF`）。浮かない・沈まない | `.navitem:hover{background:var(--navhov)}` の1箇所のみ実装 |
| **強調** | **使わない。** 色は状態色（良/注意/危険/情報）だけが担う | マーカー・色替え見出しなし。一致 |
| focus | `:focus-visible` を必ず定義する | 社内ツール3本とも実測**0**＝キーボード操作の穴 |

## 6. 二重管理の警告（**片方だけ直すと必ず壊れる**）

| # | 二重管理の実体 | 直す時に同時に触るファイル |
|---|---|---|
| 1 | **ISOイラストの色** — CSS変数が使えないため `.mjs` が直値を持つ | `src/styles/style.css` の `--iso-*`（12本・31〜42行） ⇄ `src/lib/iso/*.mjs`（bubbles / build / chart / consult / crowd / path / phone / report / shop）＋ `src/lib/iso-illusts.mjs` |
| 2 | **サービス一覧の META配列** — JSONのcapBox数と手動同期。増やすと `undefined` で落ちる | `src/pages/service/index.astro`（32行〜の `META`） ⇄ `src/data/pages/service.json` |
| 3 | **管理画面プレビューのCSS** — 本番CSSと別実体 | `src/styles/style.css` ⇄ `public/admin/cms/preview.css` |
| 4 | **テーマ既定値** — 管理画面が上書きする6変数の初期値 | `src/data/theme.json`（`accent` `accent_dark` `base_black` `bg_tint` `radius` `font_en`） ⇄ `src/styles/style.css` の `:root`（`--green` `--green-dark` `--black` `--bg` `--radius` `--font-en`）。**同値にそろえる**（現状は一致）。§7の是正はこの2ファイルを必ず同時に直す |
| 5 | **よろず屋LPがビルド系統の外** — 共通レイアウト・トークン・JSON-LD生成・reveal機構を全部バイパス | `public/yorozuya/index.html` は単体で完結。トンマナ・サービス名の変更が永久に二重メンテになる（Astro移設は未承認） |
| 6 | **`:root` の複数宣言** — 後勝ちで初期値が死ぬ | `style.css` はトップレベルだけで4箇所（17行 / 1538行「v8追加レイヤー」/ 1917行「v9追加レイヤー」/ 2505行で `--muted` を再宣言）。ほかにメディアクエリ内が2箇所（1471行 / 1827行）。Growth `public/index.html` は2箇所（12行 / 116行で `--amber` を上書き）。**新規の変数は先頭の `:root` に足す。末尾に「vNN追加レイヤー」を積まない** |

修正の作法: **既存の定義を書き換える。末尾に追記して上書きしない。** `style.css` は v7→v26 の積層で `.hero2-title` の `font-size` が4回、`.sec-en` が3回、`--muted` が2回定義されており、冒頭の `:root` だけ読むと実効値を誤る。

## 7. サイトと資料のズレ一覧（**全件が是正未承認。勝手に直さない**）

現状の `layr.co.jp`（`style.css` / `theme.json`）は資料PDFより前に作られており、以下がズレている。**新規制作は資料側（右列）に従う。既存の書き換えは河出さんの承認を取ってから、§6-4のとおり2ファイル同時に行う。**

| # | 項目 | 現状（style.css / theme.json） | 資料（正） | 影響範囲 |
|---|---|---|---|---|
| 1 | **ブランド緑** | `--green: #0F9E96`（ティール） | `#00BF63` | サイト全ページ。**最大のズレ。ロゴ・CTA・見出しすべて** |
| 2 | 濃い緑 | `--green-dark: #0A736D` | グラデ開始 `#0499AF` は**別役割**。単色の濃緑は資料では `#008516`（文字用のみ） | CTA塗り・hover |
| 3 | グラデーション | **禁止扱い**（旧A方針） | ティール `#0499AF` → ライトグリーン `#77D55C` が**正規の意匠** | ラベル帯・番号バッジ・円枠・矢印が作れない |
| 4 | アクセント数 | 「アクセント1色」 | 主役グリーン＋役割固定サブ4色（黄 `#F6CC01` / 赤 `#FF5757` / オレンジ `#E9A32F` / 淡水色 `#D6EEF1`） | 数値強調・推奨リボン・タグが表現できない |
| 5 | 影 | 「影なし」＋実測88宣言・変数4本 | 極薄1本 `0 2px 12px 6%` | カード全般 |
| 6 | 角丸 | `--radius: 28px`（`theme.json.radius:28`） | カード `16px` | カード・パネル・ボタン |
| 7 | 本文色 | `color-mix(--black 94%, #FFF)`（`--black:#0E1A1C`） | `#333333` | 本文全般 |
| 8 | 黄の下線帯 | 定義なし | `#F2C650`（表紙見出し直下） | ヒーロー見出しの装飾 |
| 9 | 波形 | 定義なし | `#C1E392` → `#B6DD75` の有機曲線 | ヒーロー・フッターの背景 |
| 10 | 書体ウエイト | 800/900指定が89箇所 | 700まで（サイズと余白で強さを出す） | 見出し全般 |
| 11 | Dモードの名乗り色 | Growth `--teal: #0F9E96` | `#00BF63` | 社内ツールでLAYRを名乗る画面 |

**是正を実行するときの順序**: (1) `theme.json` と `style.css` の6変数を同値で書き換え → (2) `public/admin/cms/preview.css` を追随（§6-3）→ (3) `--iso-*` とイラスト `.mjs` を追随（§6-1）→ (4) 各LPの直値を確認。1〜4を分割してコミットしない。

## 8. トークン外の直値が許される例外

**原則、`:root` 外の直値hexは書かない**（現状 style.css で実測40箇所＝25種類。新規は**5種類以下**＝ゲートG7）。以下だけが例外。

| 例外 | 値 | 条件 |
|---|---|---|
| **LINEブランド緑** | `#06C755` | LINE公式ボタン・LINEアイコンのみ。**必ず変数化して1箇所に持つ**（`--line-green:#06C755`）。`--brand`（`#00BF63`）と混同しない。地の色・CTA・見出しに転用しない |
| 他社の公式ブランド色 | 各社規定値 | 公式ボタン・公式ロゴを掲出する時のみ。改変しない |
| `data:` URI 内のSVG | `%23...` | CSS変数を参照できない技術的制約。`--iso-*` など**対になるCSS変数を必ず併記**し、§6-1のルールで同時に直す |
| 外部埋め込みの内部CSS | — | フォーム・GA・地図などの提供元CSS。触らない |
| `rgba()` の透明度指定 | — | ダーク面の白文字 `rgba(255,255,255,.85)` など。ベース色が白黒の場合のみ |

`color-mix` / `oklch` による派生は例外に当たらない（推奨手段）。**派生を新しい変数として登録しない。**
