# レビュー: 公式メディアの左側メニュー / モードD / 2026-09-23

## 対象と差分

- `src/layouts/SeoWorkspace.astro` の任意サイドバー、`articles.astro` の配置。
- `KijiNavigation.astro`、`KijiWorkbench.astro`、`kiji-app.mjs` のタブ連携。
- `kiji-workbench.css`、`seo-workspace.css` の今回の差分。
- 独立した読み取りレビューを実施。P1/P2の指摘なし。

## 効いている点

- 採用ノートと同じ216pxの左メニュー、番号・余白・選択色を再利用。
- 7つの機能と確認待ち件数を表示し、主領域は選択した1画面だけを表示。
- 狭幅では上部へ移し、スマートフォンでは2列で操作できる。
- タブを開くだけで生成・公開を開始しない。入力保持・遅延読込を維持。

## 検証

| 項目 | 結果 |
|---|---|
| サイトビルド | 74ページ成功 |
| 記事制作関連テスト | 28件成功。メニュー移設・件数・縦横キー・Home/End・フォーカス・初期/同一画面内のハッシュ遷移を含む |
| ワークスペース関連テスト | 14件成功 |
| デザイン回帰ゲート | PASS |
| 変更CSS + 実際の共通トークン依存を含むゲート | G1–G10 / G16 / G17の12項目PASS |
| 1440 / 1280 / 768 / 390 / 375px | ページ横はみ出しなし。実ブラウザのスクリーンショットを確認 |
| 1920px | サイドバー216px、本文の左端216px。横はみ出しなし |
| タップ領域 | ナビ最小高さ49px以上 |
| 見出し/主領域 | h1 / main は各1つ。表示tabpanelも1つ |
| 全体概要→公式メディア | 概要にはsidebarなし、公式メディアではsidebar1つと概要選択を確認 |
| 入力保持 | 未登録のキーワード入力→計測→キーワードで入力維持。確認後に入力を消去 |
| 読込エラー | 明示エラーが表示され、他タブへ切替可能 |

ゲート用CSSは今回対象のCSSと、共通スタイルに定義された参照トークンの依存関係を結合して検査。全サイトの既存CSSを新規基準で一斉是正していない。

## 既存資産の現状

既存のNoto Sans JP、色トークン、罫線、8px角丸を維持。新色・書体・影・グラデーションは追加なし。reduced-motion / focus-visible は既存の共通定義を継承する。

## 未実施のチェック

本番データの書換え、記事生成、記事公開はUI改修の検証対象外。スクリーンショットと操作確認は外部接続しない架空データ。キーボードのキーイベント・フォーカス遷移は実コントローラの回帰テストで確認した。

PASS
