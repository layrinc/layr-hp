# 顧客導線の確認結果

- 対象: トップ `/`、LINE事業 `/service/line/`
- 1280×820、768×900、390×844で表示を確認。ブラウザーの縦スクロールバーを含む幅。
- 各幅で横方向にはみ出しなし（document clientWidthとscrollWidth一致）。
- 390pxでLINE→成約→LINEと往復し、同じスクロール位置の現在地style・円のdashoffset・文章・進捗表示が完全一致。
- 100%到達で円が閉じ、成約の説明と「みんなの笑顔へ」を表示。続く三方よしの内容へ移動可能。
- 文字倍率200%、320×668では切り替え式を解除し、全工程を縦並びで表示。横方向にはみ出しなし。
- 既存の料金シミュレーターが最初の回答から次の設問に進むことを確認。
- アプリ由来のコンソールエラーなし。確認環境のブラウザー拡張が出すmetadata送信エラーは対象外。
- npm run check: 全49ページ生成、デザインゲートの基準値悪化なし。
- Cloudflare向けの検証結果とプレビューURLはPRのCI結果を正本とする。

画像は確認用iframeの表示領域を切り出したもの。開発用ツールバーだけを非表示にし、実サイトの内容は変更していない。確認用HTMLはコミット・ビルド成果物から除去済み。
